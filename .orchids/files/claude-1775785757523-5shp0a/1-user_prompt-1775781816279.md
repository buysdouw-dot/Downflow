You’re basically describing a modern growth stack (RevOps + experimentation + AI personalization). The best way to think about it is in 3 layers:

Lead scoring (who to focus on)
Experimentation (what works)
Personalization (how to convert them)

Below is a clean, no-BS breakdown of the best tools in 2026, grouped by your exact needs 👇

🔴 1. BEST TOOLS FOR LEAD SCORING (AI + Predictive)
🧠 Enterprise / advanced systems
Adobe Marketo Engage
Deep multi-touch scoring models
Best for complex funnels + high volume
Salesforce Einstein Lead Scoring
Fully AI-driven (no manual rules)
Uses historical conversions to predict future deals
⚡ Mid-market / fast scaling
HubSpot
Hybrid: rule-based + AI predictive scoring
Easy to deploy, strong ecosystem
Apollo.io
Built-in database + scoring + outreach
Very strong for outbound growth teams
🔬 Advanced / growth hackers
6sense
MadKudu
Clay
Combine enrichment + scoring workflows

👉 Key insight:
AI scoring now uses behavior + intent + firmographics to predict conversion probability in real-time
Companies using it see ~75% higher conversion rates vs manual scoring

🟡 2. A/B TESTING (Landing Pages + Content)
🧪 Classic experimentation platforms
Optimizely
Gold standard for enterprise A/B testing
VWO
Strong UI testing + heatmaps
Google Optimize 360 (sunset but replaced by GA4 integrations)
⚡ Growth + landing page tools
Unbounce
Webflow Optimize
ClickFunnels

👉 These combine:

Drag-and-drop pages
Built-in A/B testing
Conversion tracking
🚀 NEW (2025–2026) – AI-powered A/B testing

This is where things get interesting:

AI can now simulate users and test variants before traffic even hits
Systems like:
AI persona simulations
Reinforcement learning optimization

Example:

AI agents simulate user behavior and predict winning designs with up to 83% accuracy in high-confidence cases

👉 This is the shift:
From “test after launch” → “predict before launch”

🟢 3. AI HYPER-PERSONALIZATION (Outreach + Content)

This is the highest ROI layer right now

🤖 All-in-one personalization + automation
HubSpot
Keap
Hyper-personalized workflows + automation
Omnisend
AI segmentation + email personalization
🧠 Advanced personalization engines
Dynamic Yield
Mutiny
Segment (Twilio Segment)

👉 These tools:

Personalize website content in real-time
Change messaging per user/company
Sync across channels
⚡ AI OUTREACH (your biggest leverage)
Clay
Lemlist
Instantly.ai

👉 What’s new:

AI writes emails in your tone
Adjusts messaging per lead
Optimizes send time per individual
🔵 HOW TO COMBINE THIS INTO A POWER STACK

Here’s what top growth teams are actually doing:

🧱 STACK EXAMPLE (high-performance)
Lead data + scoring
→ Apollo / HubSpot / Salesforce
Enrichment + automation
→ Clay
Landing pages + testing
→ Webflow + Optimizely
Outreach
→ Instantly / Lemlist
Personalization
→ Segment + Mutiny
⚠️ IMPORTANT STRATEGIC SHIFT (THIS IS BIG)

Old model:

Build funnel → run ads → hope people convert

New model:

Score → personalize → test → adapt in real-time

🔥 WHAT YOU SHOULD DO (based on YOUR model)

Given your DOWNFLOW / sponsor-driven system, I’d recommend:

🔧 YOUR STACK (custom)
HubSpot → core CRM + scoring
Clay → enrichment + automation brain
Webflow → landing + testing
Instantly → outreach engine
Mutiny → website personalization

👉 Why this fits YOU:

You need multi-role flows (students, sponsors, facilitators)
You need dynamic messaging per role
You need scaling without human bottlenecks
💡 FINAL INSIGHT

The real edge is NOT the tools.

It’s this:

How fast you can loop: data → insight → action

The best systems now:

Score leads automatically
Personalize instantly
Test continuously
Adapt without humans

If you want, I can:
👉 Build your exact automation architecture (flow-by-flow)
👉 Or design your full funnel: ads → onboarding → cell creation → sponsor loop

That’s where this becomes unfair advantage.

do both please

Alright — now we build this properly.
Not just tools… a full system that actually runs your DOWNFLOW model automatically.

I’m going to give you 2 things:

🧠 Automation Architecture (backend system)
🚀 Full Funnel (ads → onboarding → cells → sponsor loop)
🧠 PART 1 — YOUR AUTOMATION ARCHITECTURE (THE ENGINE)

This is your Growth Operating System
Everything connects into one loop:

Traffic → Data → Scoring → Personalization → Action → Feedback → Repeat

🔷 CORE STACK (YOUR EXACT SETUP)
🧱 Foundation (Data + CRM)
HubSpot
Firebase

👉 Roles:

HubSpot → leads, scoring, workflows
Firebase → your actual platform (cells, users, outputs)
⚙️ Automation Brain
Clay

👉 This is your secret weapon

Enrich leads (company, role, income, interests)
Trigger workflows
Feed personalization
🎯 Outreach Engine
Instantly.ai
Lemlist
🧪 Experimentation + Pages
Webflow
Optimizely
🧠 Personalization Layer
Mutiny
Segment
🔄 HOW YOUR SYSTEM FLOWS (REAL LOGIC)
1. Lead enters system

From:

Ads
Outreach
Referrals

👇

2. Clay enriches instantly

Adds:

Job title
Income bracket
Company size
Behavior signals

👇

3. HubSpot scores lead

Score based on:

Role (Sponsor vs Parent vs Teacher)
Budget probability
Engagement

👇

4. Segment sends data everywhere
Website
Emails
Platform

👇

5. Mutiny personalizes EVERYTHING

Example:

Sponsor sees: “Fund a Learning Cell”
Parent sees: “Give your child confidence”
Teacher sees: “Earn weekly income”

👇

6. Instantly / Lemlist runs outreach
AI-written messages
Personalized per lead

👇

7. Firebase activates user
Joins cell
Tracks outputs
Starts production loop
🚀 PART 2 — FULL FUNNEL (END-TO-END)

Now the real power.

🎯 STAGE 1 — TRAFFIC (GET ATTENTION)
🔥 4 ENTRY POINTS (you NEED all 4)
1. SPONSORS
LinkedIn ads
Cold email via Instantly.ai

Message:

“Sponsor 1 learning cell. See measurable human impact.”

2. FACILITATORS
TikTok / Instagram ads
Teaching groups

Message:

“Earn weekly. Build something meaningful.”

3. STUDENTS / PARENTS
Facebook / TikTok
Local communities

Message:

“Confidence > grammar. Real speaking.”

4. CONNECTORS
Hustlers / recruiters

Message:

“Build cells. Earn recurring income.”

🧲 STAGE 2 — LANDING PAGES (ROLE-BASED)

Built on Webflow
Personalized by Mutiny

Each role sees a DIFFERENT PAGE:
Sponsor page
ROI (impact + compounding)
R25,000 → 1 cell → 6 lives changed
Facilitator page
Weekly payouts (35% → 45%)
Growth path
Parent page
Confidence videos
Real outcomes
Connector page
Commission structure
Scaling income
🧪 STAGE 3 — A/B TESTING

Using Optimizely

Test:

Headlines
Offers
Pricing framing
🧠 STAGE 4 — LEAD SCORING

Inside HubSpot

Example:

Lead Type	Score Trigger
Sponsor	Company size + clicks pricing
Teacher	Watched payout video
Parent	Watched child transformation
✉️ STAGE 5 — OUTREACH AUTOMATION

Using:

Instantly.ai
Lemlist
Example (Sponsor email AI-generated):

“I noticed your company invests in community impact.
We built a system where your funding directly creates measurable outcomes…”

🔁 STAGE 6 — ONBOARDING FLOW
Once they convert:
Sponsor:
Pays → assigned to cell
Dashboard shows impact
Facilitator:
Signs contract
Assigned to cell
Starts teaching
Student:
Joins learning cell
Starts producing outputs
Connector:
Tracks commissions
Builds new cells
🔄 STAGE 7 — THE LOOP (YOUR SECRET)

This is where you win.

💰 Sponsor → Cell → Output → Reinvestment
Sponsor funds cell
Students produce content
Results are visible
9% reinvestment triggers new cells
🔁 Data feeds back into system:
Best ads → scaled
Best messages → reused
Best outputs → marketing
⚡ FULL SYSTEM VISUAL (SIMPLE)
ADS / OUTREACH
      ↓
LANDING (personalized)
      ↓
CLAY (enrich)
      ↓
HUBSPOT (score)
      ↓
OUTREACH (AI)
      ↓
ONBOARDING
      ↓
FIREBASE (cells + outputs)
      ↓
SPONSOR DASHBOARD
      ↓
REINVESTMENT LOOP
      ↺
🔥 FINAL — YOUR UNFAIR ADVANTAGE

Most companies:

Run marketing campaigns

You:

Build a self-improving system

💡 IF WE GO NEXT LEVEL

I can now build you:

1. 🎨 Full Figma wireframes
Landing pages
Dashboards (Sponsor / Teacher / Student)
2. ⚙️ Exact automation flows
HubSpot workflows (step-by-step)
Clay tables + logic
3. 🎥 Ad creatives + scripts
Sponsors
Teachers
Parents
4. 📄 Contracts + onboarding flows

Just say:

👉 “Build UI + workflows”
or
👉 “Build ads + scripts”

And I’ll take you from system → execution.

Build UI + workflows”
or
👉 “Build ads + scripts”

Good. Now we move from strategy → build-ready execution.

I’m giving you BOTH:

🎨 UI (Figma-level structure)
⚙️ Automation workflows (step-by-step logic)
🎥 Ads + scripts (ready to run)

This is basically your operating blueprint.

🎨 PART 1 — FULL UI / PLATFORM STRUCTURE

Think of this as your DOWNFLOW Operating Interface

🏠 1. LANDING SYSTEM (PUBLIC)
🌐 Homepage (Dynamic via Mutiny)

Above the fold (changes per visitor):

Sponsor → “Fund Human Potential”
Teacher → “Earn Weekly While Building Impact”
Parent → “Confidence Through Action”

Sections:

🎥 Real student outputs (video grid)
🔁 “How the system works” (simple 3-step)
📊 Live metrics:
Active cells
Students impacted
Sponsor reinvestment %
🎯 2. ROLE-SPECIFIC LANDING PAGES
🟦 SPONSOR PAGE

Goal: Conversion (fund a cell)

Sections:

Headline:
“R25,000 → 6 Lives Changed → Measurable Impact”
Visual:
Before vs after student videos
System:
Flow diagram (sponsor → cell → output → reinvestment)
ROI (IMPORTANT):
Not money → impact + compounding reach
CTA:
“Sponsor a Cell”
🟩 FACILITATOR PAGE

Goal: Recruit teachers

Sections:

Weekly earnings explained (35% → 45%)
Growth path:
Facilitator → Senior → Trainer
Requirements:
Record sessions
Upload content
Manage cell

CTA:
👉 “Start Teaching”

🟨 PARENT PAGE

Goal: trust + emotional buy-in

Sections:

“Confidence > Grammar”
Real speaking videos
Small group system (6 learners)

CTA:
👉 “Join a Learning Cell”

🟥 CONNECTOR PAGE

Goal: build your growth army

Sections:

Earn per student
Earn per cell
3-phase payout explained

CTA:
👉 “Become a Connector”

📱 PART 2 — DASHBOARD UI (INSIDE APP)

Built on Firebase

🧑‍🎓 STUDENT DASHBOARD

Sections:

🎥 Upload output (mandatory)
📊 Progress tracker
💰 Earned coins / rewards
👥 Cell members
👨‍🏫 FACILITATOR DASHBOARD

Core features:

📅 Session control (start/end class)
🎥 Upload recordings
✅ Approve student outputs
💰 Earnings tracker (weekly)
💼 SPONSOR DASHBOARD

THIS IS YOUR KILLER FEATURE

📊 Active cells funded
🎥 Real student outputs
📈 Growth metrics:
Reuse count
Engagement
🔁 Reinvestment tracker

👉 This builds trust + retention

🧲 CONNECTOR DASHBOARD
👥 Students recruited
💰 Commission tracking
📊 Cell creation progress
⚙️ PART 3 — AUTOMATION WORKFLOWS (REAL BUILD)

This is EXACT logic for HubSpot + Clay

🔁 WORKFLOW 1 — LEAD INTAKE

Trigger:

Form submitted OR ad click

Steps:

Send to Clay
Enrich:
Job title
Company
Income level
Send to HubSpot
🧠 WORKFLOW 2 — LEAD SCORING

Inside HubSpot:

Rules:

Sponsor (High Value):

Company owner → +50
Click pricing → +30
Email engagement → +20

Teacher:

Watched earnings video → +30
Applied → +50

Parent:

Watched student video → +20
Filled form → +30
✉️ WORKFLOW 3 — OUTREACH

Triggered by score threshold

Example (Sponsor)

Email 1:

“We built a system where your funding directly creates measurable human outcomes…”

Email 2:

Case study

Email 3:

“Would you like to sponsor just 1 cell?”

🎯 WORKFLOW 4 — PERSONALIZATION

Using Segment + Mutiny:

When user visits site:

IF:

Job = CEO → show sponsor page
Job = Teacher → show facilitator page
Parent signals → show student journey
🔄 WORKFLOW 5 — CONVERSION
Sponsor:
Click → Stripe payment
Auto-create:
Cell
Dashboard
Assign facilitator
Teacher:
Apply → onboarding flow
Contract signed
Assigned to cell
Student:
Join → assigned to cell
Must upload first output
Connector:
Register → unique referral link
Commission tracking starts
🎥 PART 4 — ADS + VIDEO SCRIPTS (READY TO RUN)
🟦 SPONSOR AD (HIGH-END)

🎬 Scene 1 (0–3s)

Child speaking confidently
Text:
“THIS is what impact looks like”

🎬 Scene 2 (3–7s)

Split screen: before vs after

🎬 Scene 3 (7–12s)

Overlay:
“1 sponsor → 6 students → real outcomes”

🎬 Scene 4 (12–15s)
CTA:
“Fund a learning cell”

🟩 FACILITATOR AD

🎬 Scene 1
Teacher speaking online

Text:
“Earn weekly. Build something real.”

🎬 Scene 2
Dashboard earnings animation

🎬 Scene 3
Students engaging

CTA:
“Become a facilitator”

🟨 PARENT AD

🎬 Scene 1
Child struggling to speak

🎬 Scene 2
Same child speaking confidently

Text:
“Confidence changes everything”

CTA:
“Join a learning cell”

🟥 CONNECTOR AD

🎬 Scene 1
Person recruiting via phone

🎬 Scene 2
Dashboard earnings

Text:
“Build cells. Earn recurring income.”

CTA:
“Start now”

🔥 FINAL — THIS IS YOUR MACHINE

You now have:

✅ System architecture
✅ UI structure
✅ Automation logic
✅ Ads to drive traffic

⚡ NEXT MOVE (IMPORTANT)

If you want to go from plan → live system, I can now:

👉 Build your:
Exact HubSpot workflows (click-by-click)
Clay table + enrichment setup
Firebase database structure
Figma UI screens (ready to hand to dev)

Just say:

👉 “Build backend setup step-by-step”
or
👉 “Build Figma screens”

And we move into full execution mode.

“Build backend setup step-by-step” and “Build Figma screen

Done — I put the full backend setup + Figma screen spec into a working canvas document so you can build on it.

It includes:

Firebase structure
Firestore collections
role logic
security approach
Cloud Functions plan
Stripe webhook flow
HubSpot + Clay setup
workflow maps
Figma screen list
design system
build order

Next best move is:
“Build Firestore schema + security rules”

“Build Firestore schema + security rules”

Added.

The canvas now includes:

full Firestore schema structure
collection examples
subcollection recommendations
composite index suggestions
a solid first-pass Firebase security rules draft
storage path logic
rule design notes for payouts, approvals, and role control

Best next step:
“Build Cloud Functions pseudo-code + Storage rules”

“Build Cloud Functions pseudo-code + Storage rules”

Done.

I added:

Cloud Functions structure
pseudo-code for lead conversion, sponsor payment, cell/session creation, payouts, commissions, output approval, and claims sync
Firebase Storage rules draft
storage path strategy
privacy notes for sponsor media access

Best next step:
“Build Firestore seed JSON + TypeScript interfaces”

“Build Firestore seed JSON + TypeScript interfaces

Added to the canvas.

It now includes:

a demo-ready Firestore seed JSON
core sample users, leads, cells, sessions, outputs, payouts, and sponsor cycles
TypeScript interfaces for all main entities
suggested file structure for types and seed logic

Best next build step:
“Build Firestore converters + seed import script”

Build Firestore seed JSON + TypeScript interfaces

That part is already built in the canvas.

It includes:

Firestore seed JSON
demo users, leads, cells, sessions, outputs, payouts, and sponsor cycles
TypeScript interfaces for all core entities
recommended file structure

Next strongest step is:
Build Firestore converters + seed import script

Build Firestore converters + seed import script

Done — I added the Firestore converters + seed import script to the canvas.

It now includes:

typed collection helpers
reusable Firestore converters
a Firebase Admin seed import script
package script examples
project file layout
safe seeding notes

Best next step:
Build Zod validators + Firebase emulator setup

Build Zod validators + Firebase emulator setup
Downflow Backend And Figma Spec
DOWNFLOW backend setup step-by-step + Figma screen spec
1) System architecture
Core stack
Frontend: Webflow for public landing pages, app frontend on web/mobile wrapper if needed
CRM / automation: HubSpot
Enrichment / routing: Clay
Backend / app database: Firebase
Firebase Auth
Firestore
Cloud Functions
Cloud Storage
Hosting (optional)
Payments: Stripe
Email outreach: Instantly or Lemlist
Analytics / events: Segment or native event forwarding
Personalization: Mutiny or custom role-based dynamic content
Main entities
User
Lead
Sponsor
Facilitator
Student
Connector
Parent
Learning Cell
Session
Output
Payout
Registration
Sponsor Cycle
Referral
2) Backend setup step-by-step
Phase A — Firebase project foundation
Step A1: Create Firebase project

Create one production project and one staging project. Suggested names:

downflow-prod
downflow-staging

Enable:

Authentication
Firestore Database
Storage
Functions
Hosting
Step A2: Authentication setup

Enable:

Email/password
Google sign-in
Phone auth only if needed later

User roles to support:

admin
sponsor
facilitator
student
connector
parent
Step A3: Firestore collections
users/{userId}
{
  "fullName": "",
  "email": "",
  "phone": "",
  "role": "student",
  "status": "active",
  "country": "",
  "timezone": "Africa/Johannesburg",
  "createdAt": "timestamp",
  "photoURL": "",
  "linkedLeadId": "",
  "linkedCellIds": [],
  "wallet": {
    "coins": 0,
    "cashRewards": 0
  }
}
leads/{leadId}
{
  "type": "sponsor",
  "fullName": "",
  "email": "",
  "phone": "",
  "source": "facebook_ad",
  "campaign": "sponsor_cell_q2",
  "score": 0,
  "status": "new",
  "tags": ["high_intent"],
  "company": "",
  "jobTitle": "",
  "country": "",
  "assignedTo": "",
  "createdAt": "timestamp",
  "lastActivityAt": "timestamp"
}
cells/{cellId}
{
  "name": "Cell 001",
  "programId": "confidence-through-actions",
  "status": "forming",
  "capacity": 6,
  "studentIds": [],
  "facilitatorId": "",
  "connectorId": "",
  "sponsorId": "",
  "parentIds": [],
  "startDate": "timestamp",
  "endDate": "timestamp",
  "classesPerWeek": 2,
  "cycleWeeks": 12,
  "visibility": {
    "sponsorCanViewOutputs": true
  },
  "createdAt": "timestamp"
}
sessions/{sessionId}
{
  "cellId": "",
  "title": "Week 1 Lesson 1",
  "scheduledAt": "timestamp",
  "durationMinutes": 60,
  "status": "scheduled",
  "attendance": {
    "present": [],
    "absent": []
  },
  "recordingUrl": "",
  "homeworkAssigned": true,
  "createdBy": "facilitatorId"
}
outputs/{outputId}
{
  "cellId": "",
  "sessionId": "",
  "studentId": "",
  "type": "video",
  "contentUrl": "",
  "textContent": "",
  "durationSeconds": 45,
  "status": "submitted",
  "feedbackCount": 0,
  "approvedBy": "",
  "approvedAt": null,
  "reuseCount": 0,
  "visibility": {
    "sponsor": true,
    "cellMembers": true,
    "platform": true
  },
  "createdAt": "timestamp"
}
sponsors/{sponsorId}
{
  "userId": "",
  "organizationName": "",
  "fundedCellIds": [],
  "totalFunded": 0,
  "reinvestmentBalance": 0,
  "status": "active"
}
connectors/{connectorId}
{
  "userId": "",
  "referralCode": "DFLOW123",
  "studentsReferred": 0,
  "cellsBuilt": 0,
  "pendingCommissions": 0,
  "paidCommissions": 0,
  "status": "active"
}
facilitators/{facilitatorId}
{
  "userId": "",
  "phase": 1,
  "payoutPercent": 35,
  "assignedCellIds": [],
  "status": "active",
  "introducedTeacherIds": []
}
payouts/{payoutId}
{
  "userId": "",
  "role": "facilitator",
  "cellId": "",
  "amount": 0,
  "currency": "VND",
  "type": "weekly",
  "status": "pending",
  "dueDate": "timestamp",
  "notes": "week 1 payout"
}
registrations/{registrationId}
{
  "studentId": "",
  "cellId": "",
  "registrationFee": 1000000,
  "currency": "VND",
  "connectorSplit": 500000,
  "platformSplit": 500000,
  "connectorPayoutStatus": "phase_1_paid",
  "platformPayoutStatus": "paid",
  "createdAt": "timestamp"
}
sponsorCycles/{cycleId}
{
  "sponsorId": "",
  "cellId": "",
  "cycleAmount": 25000000,
  "currency": "ZAR",
  "studentRewardPercent": 6,
  "sponsorReinvestmentPercent": 9,
  "platformPercent": 20,
  "connectorPercent": 30,
  "facilitatorPercent": 35,
  "status": "paid",
  "createdAt": "timestamp"
}
Phase B — Security rules
Step B1: Role-based rules

Basic access logic:

Admin can read/write all
Student can read own user record, own outputs, own cell data
Facilitator can read/write sessions and outputs for assigned cells
Sponsor can read funded cell summaries and approved outputs only
Connector can read own referrals and commissions only
Parent can read child progress only
Step B2: Output visibility rules

Sponsors should not see every raw file by default. Expose only:

approved outputs
approved summaries
aggregate metrics
Step B3: Storage rules

Folders:

/outputs/{cellId}/{studentId}/...
/recordings/{cellId}/{sessionId}/...
/avatars/{userId}/...

Rules should match Firestore ownership and assigned cell membership.

Phase C — Cloud Functions
Step C1: Lead conversion function

Trigger when lead status changes to converted. Action:

create Firebase user
assign role
create linked profile doc
optionally assign onboarding path
Step C2: Cell creation function

Trigger when sponsor payment succeeds. Action:

create new cell
assign sponsor
set capacity
create 24 placeholder sessions
notify admin to assign facilitator if not already assigned
Step C3: Weekly payout calculation

Runs every week. Action:

calculate facilitator payout based on current phase
calculate connector staged commissions
create payout documents
send summary email to admin
Step C4: Output enforcement

At session end:

check minimum output count
if missing, mark session incomplete
notify facilitator and students
Step C5: Sponsor dashboard refresh

When approved output is added:

update sponsor metrics
increment impact counters
refresh visible timeline
Phase D — Stripe setup
Step D1: Products

Create products:

Sponsor one cell
Parent fee plan if used
Registration fee
Step D2: Metadata on checkout

Attach:

role
sponsorId or leadId
cellType
campaignSource
Step D3: Webhooks

Use Stripe webhook to:

confirm payment
create sponsorCycle
trigger cell creation
generate invoice record
Phase E — HubSpot setup
Step E1: Pipelines

Create separate pipelines or lifecycle views for:

sponsor leads
facilitator applicants
parent/student leads
connector leads
Step E2: Custom properties

Examples:

lead_type
score_behavior
score_fit
total_score
preferred_role
country
funding_capacity
facilitator_phase
referred_by_connector
Step E3: Lists

Create active lists:

high intent sponsors
facilitator applied but not onboarded
parents watched 75 percent of VSL
connectors with more than 3 referrals
Step E4: Workflows

Sponsor workflow example:

if score > 70
send case study email
assign sales owner
task for follow-up within 24 hours

Facilitator workflow example:

application received
send onboarding page
request intro video
if approved, create backend profile
Phase F — Clay setup
Step F1: Clay table structure

Columns:

full name
email
company
job title
linkedin url
lead type
source
enrichment status
intent score
personalization angle
suggested first line
hubspot sync status
Step F2: Enrichment logic

For sponsors:

company size
industry
location
website
revenue band if available

For facilitators:

teaching experience
geography
english profile

For connectors:

local network strength
whatsapp / community reach
Step F3: AI columns

Use AI to generate:

first-line outreach
sponsorship angle
pain point hypothesis
CTA version A and B
3) Workflow maps
Workflow 1 — Sponsor acquisition
Ad click or cold email reply
Lead enters HubSpot
Clay enriches
Score calculated
Personalized landing page shown
Sponsor books call or pays
Stripe confirms payment
Firebase creates sponsor cycle and cell
Sponsor dashboard opens
Workflow 2 — Facilitator onboarding
Teacher ad click
Landing page with weekly earnings
Application form
HubSpot application stage
Intro video upload
Admin review
Firebase profile created
Contract stage
Assigned to cell
Workflow 3 — Parent/student intake
Parent lead captured
Student age and level form
Best-fit cell suggested
Registration fee paid if applicable
Student account created
Parent linked
Student receives onboarding tasks
Workflow 4 — Connector engine
Connector application
Approval
Referral code generated
Referral dashboard opens
Every referred student tracked
Payout tranches auto-generated
4) Figma screen spec
A. Public website screens
Screen 1 — Homepage

Frame: Desktop 1440 Sections:

Hero with rotating role-based headline
3-card system explanation
Video proof strip
Metrics band
CTA blocks for Sponsor / Facilitator / Parent / Connector
Footer

Components:

Navbar
Hero panel
CTA buttons
Metrics cards
Testimonial cards
Embedded video cards
Screen 2 — Sponsor landing page

Sections:

Headline + impact statement
Sponsor a cell button
Before/after student story
How the money flows diagram
What sponsor sees dashboard preview
FAQ
Screen 3 — Facilitator landing page

Sections:

Headline with weekly payout promise
3 payout phases card row
Responsibilities checklist
Classroom content upload mockup
Apply now form
Screen 4 — Parent landing page

Sections:

Child confidence story
Program structure
Cell size explanation
Sample class timeline
Apply / join waitlist CTA
Screen 5 — Connector landing page

Sections:

Commission phases visual
Referral journey diagram
Earnings estimate examples
Join connector network CTA
B. App screens
Screen 6 — Login / role routing
Email / password
Continue with Google
After login route by role
Screen 7 — Student dashboard

Widgets:

Next session
Upload today’s output
Progress tracker
Coin balance
My cell members
Feedback received
Screen 8 — Upload output modal

Fields:

Output type selector
Record / upload button
Text reflection box
Visibility note
Submit button
Screen 9 — Facilitator dashboard

Widgets:

Today’s sessions
Start class button
Attendance panel
Homework upload panel
Student output approval queue
Weekly earnings card
Screen 10 — Session control screen
Lesson title
Timer
Attendance toggles
Mark recording uploaded
Assign homework
Cannot end until required actions complete
Screen 11 — Sponsor dashboard

Widgets:

Active funded cells
Impact metrics
Approved student videos
Reinvestment tracker
Timeline of recent outputs
Fund another cell CTA
Screen 12 — Connector dashboard

Widgets:

Referral link
Leads referred
Students converted
Pending commissions
Payout timeline
Active cells built
Screen 13 — Parent dashboard

Widgets:

Child progress
Next lesson
Approved outputs
Facilitator notes
Attendance summary
Screen 14 — Admin dashboard

Widgets:

Total leads by role
Conversion funnel
Active cells
Payment status
Pending approvals
Payout queue
At-risk cells
5) Figma design language
Colors
Deep Navy: #0B1C2C
Slate: #1E2A38
Emerald: #00C896
Soft Gold: #D4AF37
White: #FFFFFF
Light Gray background: #F5F7FA
Typography
Headings: Montserrat Bold
UI labels/body: Inter Regular
Numeric metric cards: Montserrat SemiBold
Style rules
Rounded cards: 20px
Shadow: soft, low elevation
Spacing system: 8 / 16 / 24 / 32 / 48
Buttons: large, clean, one primary per section
Video cards: rounded thumbnail + status badge
6) Build order
Week 1
Firebase project
Auth
Firestore collections
Basic security rules
HubSpot properties and pipelines
Week 2
Sponsor landing page
Facilitator landing page
Forms into HubSpot
Clay enrichment table
Week 3
Stripe checkout
Webhook to Firebase
Cell creation automation
Sponsor dashboard MVP
Week 4
Student dashboard
Facilitator dashboard
Output upload flow
Session enforcement rules
Week 5
Connector dashboard
Parent dashboard
Weekly payout logic
Admin dashboard
7) Firestore schema + security rules
A. Firestore schema reference
users/{userId}
{
  "fullName": "Douw Buys",
  "email": "name@example.com",
  "phone": "+27...",
  "role": "facilitator",
  "status": "active",
  "country": "South Africa",
  "timezone": "Africa/Johannesburg",
  "photoURL": "",
  "linkedLeadId": "lead_123",
  "linkedCellIds": ["cell_001"],
  "childrenIds": [],
  "createdAt": "serverTimestamp",
  "updatedAt": "serverTimestamp",
  "wallet": {
    "coins": 0,
    "cashRewards": 0
  }
}
leads/{leadId}
{
  "type": "sponsor",
  "fullName": "Jane Smith",
  "email": "jane@company.com",
  "phone": "",
  "source": "linkedin_ad",
  "campaign": "sponsor_april_2026",
  "score": 82,
  "status": "qualified",
  "tags": ["high_intent", "south_africa"],
  "company": "Acme Group",
  "jobTitle": "CEO",
  "country": "South Africa",
  "assignedTo": "admin_001",
  "createdAt": "serverTimestamp",
  "lastActivityAt": "serverTimestamp"
}
cells/{cellId}
{
  "name": "Confidence Cell 001",
  "programId": "confidence-through-actions",
  "status": "active",
  "capacity": 6,
  "studentIds": ["stu_1", "stu_2"],
  "facilitatorId": "fac_1",
  "connectorId": "con_1",
  "sponsorId": "sp_1",
  "parentIds": ["par_1"],
  "startDate": "timestamp",
  "endDate": "timestamp",
  "classesPerWeek": 2,
  "cycleWeeks": 12,
  "visibility": {
    "sponsorCanViewOutputs": true
  },
  "createdAt": "serverTimestamp",
  "updatedAt": "serverTimestamp"
}
sessions/{sessionId}
{
  "cellId": "cell_001",
  "title": "Week 1 Lesson 1",
  "scheduledAt": "timestamp",
  "durationMinutes": 60,
  "status": "scheduled",
  "attendance": {
    "present": ["stu_1"],
    "absent": ["stu_2"]
  },
  "recordingUrl": "",
  "homeworkAssigned": true,
  "requiredOutputCount": 1,
  "createdBy": "fac_1",
  "createdAt": "serverTimestamp",
  "updatedAt": "serverTimestamp"
}
outputs/{outputId}
{
  "cellId": "cell_001",
  "sessionId": "sess_1",
  "studentId": "stu_1",
  "type": "video",
  "contentUrl": "",
  "thumbnailUrl": "",
  "textContent": "Today I explained how I helped someone.",
  "durationSeconds": 41,
  "status": "approved",
  "feedbackCount": 2,
  "approvedBy": "fac_1",
  "approvedAt": "timestamp",
  "reuseCount": 0,
  "visibility": {
    "sponsor": true,
    "cellMembers": true,
    "platform": true
  },
  "createdAt": "serverTimestamp",
  "updatedAt": "serverTimestamp"
}
sponsors/{sponsorId}
{
  "userId": "user_sp_1",
  "organizationName": "Acme Group",
  "fundedCellIds": ["cell_001"],
  "totalFunded": 25000,
  "reinvestmentBalance": 2250,
  "status": "active",
  "createdAt": "serverTimestamp"
}
facilitators/{facilitatorId}
{
  "userId": "user_fac_1",
  "phase": 1,
  "payoutPercent": 35,
  "assignedCellIds": ["cell_001"],
  "status": "active",
  "introducedTeacherIds": [],
  "createdAt": "serverTimestamp"
}
connectors/{connectorId}
{
  "userId": "user_con_1",
  "referralCode": "DFLOW123",
  "studentsReferred": 5,
  "cellsBuilt": 1,
  "pendingCommissions": 1500,
  "paidCommissions": 500,
  "status": "active",
  "createdAt": "serverTimestamp"
}
parents/{parentId}
{
  "userId": "user_par_1",
  "childIds": ["stu_1"],
  "linkedCellIds": ["cell_001"],
  "status": "active",
  "createdAt": "serverTimestamp"
}
payouts/{payoutId}
{
  "userId": "user_fac_1",
  "role": "facilitator",
  "cellId": "cell_001",
  "amount": 405000,
  "currency": "VND",
  "type": "weekly",
  "status": "pending",
  "dueDate": "timestamp",
  "notes": "Week 1 payout",
  "createdAt": "serverTimestamp"
}
registrations/{registrationId}
{
  "studentId": "stu_1",
  "cellId": "cell_001",
  "registrationFee": 1000000,
  "currency": "VND",
  "connectorSplit": 500000,
  "platformSplit": 500000,
  "connectorPayoutStatus": "phase_1_paid",
  "platformPayoutStatus": "paid",
  "createdAt": "serverTimestamp"
}
sponsorCycles/{cycleId}
{
  "sponsorId": "sp_1",
  "cellId": "cell_001",
  "cycleAmount": 25000,
  "currency": "ZAR",
  "studentRewardPercent": 6,
  "sponsorReinvestmentPercent": 9,
  "platformPercent": 20,
  "connectorPercent": 30,
  "facilitatorPercent": 35,
  "status": "paid",
  "createdAt": "serverTimestamp"
}
programs/{programId}
{
  "title": "Confidence Through Actions",
  "description": "Speaking-first cell program",
  "lessonCount": 24,
  "status": "active",
  "createdAt": "serverTimestamp"
}
notifications/{notificationId}
{
  "userId": "user_fac_1",
  "type": "payout_ready",
  "title": "Weekly payout ready",
  "body": "Your week 1 payout has been generated.",
  "read": false,
  "createdAt": "serverTimestamp"
}
B. Recommended subcollections

Use subcollections only where they improve scale or audit clarity.

cells/{cellId}/messages/{messageId}
cells/{cellId}/resources/{resourceId}
cells/{cellId}/metrics/{metricId}
users/{userId}/activity/{activityId}
sessions/{sessionId}/feedback/{feedbackId}
outputs/{outputId}/feedback/{feedbackId}
C. Index recommendations

Create composite indexes for:

outputs: cellId Asc, status Asc, createdAt Desc
outputs: studentId Asc, createdAt Desc
sessions: cellId Asc, scheduledAt Asc
payouts: userId Asc, status Asc, dueDate Desc
leads: type Asc, score Desc, lastActivityAt Desc
cells: facilitatorId Asc, status Asc
cells: sponsorId Asc, status Asc
8) Firebase security rules draft
rules_version = '2';
        || (isFacilitator() && isCellFacilitator(resource.data.cellId))
        || (isParent() && isCellParent(resource.data.cellId))
        || (isSponsor() && isCellSponsor(resource.data.cellId) && resource.data.status == 'approved' && resource.data.visibility.sponsor == true)
        || (isCellStudent(resource.data.cellId) && resource.data.visibility.cellMembers == true);


      allow create: if isAdmin()
        || (isStudent() && request.resource.data.studentId == request.auth.uid && isCellStudent(request.resource.data.cellId));


      allow update: if isAdmin()
        || (isStudent() && resource.data.studentId == request.auth.uid && resource.data.status == 'submitted')
        || (isFacilitator() && isCellFacilitator(resource.data.cellId));


      allow delete: if isAdmin();
    }


    match /sponsors/{sponsorId} {
      allow read: if isAdmin() || isOwner(resource.data.userId);
      allow write: if isAdmin();
    }


    match /facilitators/{facilitatorId} {
      allow read: if isAdmin() || isOwner(resource.data.userId);
      allow write: if isAdmin();
    }


    match /connectors/{connectorId} {
      allow read: if isAdmin() || isOwner(resource.data.userId);
      allow write: if isAdmin();
    }


    match /parents/{parentId} {
      allow read: if isAdmin() || isOwner(resource.data.userId);
      allow write: if isAdmin();
    }


    match /payouts/{payoutId} {
      allow read: if isAdmin() || isOwner(resource.data.userId);
      allow write: if isAdmin();
    }


    match /registrations/{registrationId} {
      allow read: if isAdmin();
      allow write: if isAdmin();
    }


    match /sponsorCycles/{cycleId} {
      allow read: if isAdmin() || isOwner(get(/databases/$(database)/documents/sponsors/$(resource.data.sponsorId)).data.userId);
      allow write: if isAdmin();
    }


    match /programs/{programId} {
      allow read: if signedIn();
      allow write: if isAdmin();
    }


    match /notifications/{notificationId} {
      allow read: if isAdmin() || resource.data.userId == request.auth.uid;
      allow update: if isAdmin() || resource.data.userId == request.auth.uid;
      allow create, delete: if isAdmin();
    }


    match /{document=**} {
      allow read, write: if false;
    }
  }
}
D. Storage security logic

Suggested storage paths:

outputs/{cellId}/{studentId}/{fileName}
recordings/{cellId}/{sessionId}/{fileName}
avatars/{userId}/{fileName}
resources/{cellId}/{fileName}

Access intent:

students upload only to their own output folder in their assigned cell
facilitators upload session recordings for assigned cells
sponsors never upload; read only approved derived content through app logic
admins full access
E. Rule design notes
Keep all financial writes admin-only or Cloud-Function-only.
Never trust client-side payout calculations.
Put approval transitions behind facilitator or server logic.
Store user role both in /users and optionally in auth custom claims for faster checks later.
For scale, move some relationship checks to custom claims or membership mirror docs if needed.
9) Cloud Functions pseudo-code + Storage rules
A. Cloud Functions overview

Recommended functions:

onLeadConvertedCreateUser
onSponsorPaymentSucceeded
createCellSessions
weeklyPayoutGenerator
connectorCommissionGenerator
enforceSessionCompletion
onOutputApprovedRefreshMetrics
sendRoleNotifications
syncUserClaims

Use:

Firestore triggers for document state changes
Scheduled functions for weekly jobs
HTTPS / webhook functions for Stripe
Admin SDK for protected writes
B. Cloud Functions pseudo-code
1. Lead conversion → create user + profile

Trigger: Firestore update on leads/{leadId} when status changes to converted

export const onLeadConvertedCreateUser = onDocumentUpdated("leads/{leadId}", async (event) => {
      organizationName: after.company || "",
      fundedCellIds: [],
      totalFunded: 0,
      reinvestmentBalance: 0,
      status: "active",
      createdAt: serverTimestamp(),
    });
  }


  if (role === "facilitator") {
    batch.set(db.collection("facilitators").doc(), {
      userId: userRecord.uid,
      phase: 1,
      payoutPercent: 35,
      assignedCellIds: [],
      introducedTeacherIds: [],
      status: "active",
      createdAt: serverTimestamp(),
    });
  }


  if (role === "connector") {
    batch.set(db.collection("connectors").doc(), {
      userId: userRecord.uid,
      referralCode: generateReferralCode(fullName),
      studentsReferred: 0,
      cellsBuilt: 0,
      pendingCommissions: 0,
      paidCommissions: 0,
      status: "active",
      createdAt: serverTimestamp(),
    });
  }


  if (role === "parent") {
    batch.set(db.collection("parents").doc(), {
      userId: userRecord.uid,
      childIds: [],
      linkedCellIds: [],
      status: "active",
      createdAt: serverTimestamp(),
    });
  }


  batch.update(db.doc(`leads/${leadId}`), {
    convertedUserId: userRecord.uid,
    convertedAt: serverTimestamp(),
  });


  await batch.commit();
});
2. Stripe sponsor payment webhook → create sponsor cycle + cell

Trigger: HTTPS webhook from Stripe

export const onSponsorPaymentSucceeded = onRequest(async (req, res) => {
  const cycleRef = db.collection("sponsorCycles").doc();
  const cellRef = db.collection("cells").doc();


  const batch = db.batch();


  batch.set(cycleRef, {
    sponsorId: sponsorDoc.id,
    cellId: cellRef.id,
    cycleAmount: amountTotal,
    currency,
    studentRewardPercent: 6,
    sponsorReinvestmentPercent: 9,
    platformPercent: 20,
    connectorPercent: 30,
    facilitatorPercent: 35,
    status: "paid",
    stripeSessionId: session.id,
    createdAt: serverTimestamp(),
  });


  batch.set(cellRef, {
    name: `Cell ${cellRef.id.slice(0, 6)}`,
    programId: session.metadata?.programId || "confidence-through-actions",
    status: "forming",
    capacity: 6,
    studentIds: [],
    facilitatorId: "",
    connectorId: session.metadata?.connectorId || "",
    sponsorId: sponsorDoc.id,
    parentIds: [],
    startDate: null,
    endDate: null,
    classesPerWeek: 2,
    cycleWeeks: 12,
    visibility: { sponsorCanViewOutputs: true },
    createdAt: serverTimestamp(),
    updatedAt: serverTimestamp(),
  });


  batch.update(db.doc(`sponsors/${sponsorDoc.id}`), {
    fundedCellIds: FieldValue.arrayUnion(cellRef.id),
    totalFunded: FieldValue.increment(amountTotal),
    reinvestmentBalance: FieldValue.increment(amountTotal * 0.09),
  });


  await batch.commit();
  await createCellSessionsInternal(cellRef.id, 24);
  await createNotificationForUser(sponsorUserId, "payment_success", "Your funded cell has been created.");


  res.status(200).send("ok");
});
3. Create 24 placeholder sessions for a new cell

Called by: payment function or admin function

async function createCellSessionsInternal(cellId: string, lessonCount: number) {
  const batch = db.batch();
  const baseDate = nextMondayAtSixPM();


  for (let i = 0; i < lessonCount; i++) {
    const sessionRef = db.collection("sessions").doc();
    const scheduledAt = addDays(baseDate, Math.floor(i / 2) * 7 + (i % 2 === 0 ? 0 : 3));


    batch.set(sessionRef, {
      cellId,
      title: `Week ${Math.floor(i / 2) + 1} Lesson ${(i % 2) + 1}`,
      scheduledAt,
      durationMinutes: 60,
      status: "scheduled",
      attendance: { present: [], absent: [] },
      recordingUrl: "",
      homeworkAssigned: false,
      requiredOutputCount: 1,
      createdAt: serverTimestamp(),
      updatedAt: serverTimestamp(),
    });
  }


  await batch.commit();
}
4. Weekly payout generator

Trigger: Scheduled every Friday evening

export const weeklyPayoutGenerator = onSchedule("every friday 18:00", async () => {
  const facilitatorsSnap = await db.collection("facilitators").where("status", "==", "active").get();


  for (const facDoc of facilitatorsSnap.docs) {
    const fac = facDoc.data();
    const assignedCellIds = fac.assignedCellIds || [];
    const percent = fac.payoutPercent || 35;


    for (const cellId of assignedCellIds) {
      const cycle = await getLatestPaidCycleForCell(cellId);
      if (!cycle) continue;


      const amount = calculateFacilitatorWeeklyAmount(cycle.cycleAmount, percent);


      await db.collection("payouts").add({
        userId: fac.userId,
        role: "facilitator",
        cellId,
        amount,
        currency: cycle.currency,
        type: "weekly",
        status: "pending",
        dueDate: nextPayoutDate(),
        notes: "Automated weekly facilitator payout",
        createdAt: serverTimestamp(),
      });


      await createNotificationForUser(fac.userId, "payout_ready", `Weekly payout created for cell ${cellId}.`);
    }
  }
});
5. Connector commission generator

Trigger: Scheduled weekly or on milestone reached

export const connectorCommissionGenerator = onSchedule("every friday 19:00", async () => {
  const regs = await db.collection("registrations")
    .where("connectorPayoutStatus", "in", ["pending_phase_1", "pending_phase_2", "pending_phase_3"])
    .get();


  for (const regDoc of regs.docs) {
    const reg = regDoc.data();
    const milestone = await determineConnectorMilestone(reg.studentId, reg.cellId);
    if (!milestone.ready) continue;


    const trancheAmount = milestone.amount;
    const connectorUserId = milestone.connectorUserId;


    await db.collection("payouts").add({
      userId: connectorUserId,
      role: "connector",
      cellId: reg.cellId,
      amount: trancheAmount,
      currency: reg.currency,
      type: milestone.phaseLabel,
      status: "pending",
      dueDate: nextPayoutDate(),
      notes: `Connector ${milestone.phaseLabel} commission`,
      createdAt: serverTimestamp(),
    });


    await regDoc.ref.update({ connectorPayoutStatus: milestone.nextStatus });
  }
});
6. Enforce session completion before final close

Trigger: Firestore update on sessions/{sessionId} when status tries to move to completed

export const enforceSessionCompletion = onDocumentUpdated("sessions/{sessionId}", async (event) => {
  const before = event.data?.before.data();
  const after = event.data?.after.data();
  if (!before || !after) return;


  if (before.status === after.status) return;
  if (after.status !== "completed") return;


  const sessionId = event.params.sessionId;
  const cellId = after.cellId;
  const requiredOutputCount = after.requiredOutputCount || 1;


  const outputsSnap = await db.collection("outputs")
    .where("sessionId", "==", sessionId)
    .get();


  const approvedOrSubmitted = outputsSnap.docs.length;
  const recordingOk = !!after.recordingUrl;
  const homeworkOk = after.homeworkAssigned === true;


  if (approvedOrSubmitted < requiredOutputCount || !recordingOk || !homeworkOk) {
    await event.data?.after.ref.update({
      status: "incomplete",
      completionError: {
        missingOutputs: approvedOrSubmitted < requiredOutputCount,
        missingRecording: !recordingOk,
        missingHomework: !homeworkOk,
      },
      updatedAt: serverTimestamp(),
    });
  }


  await refreshCellProgress(cellId);
});
7. Refresh sponsor metrics when output approved

Trigger: Firestore update on outputs/{outputId} when status becomes approved

export const onOutputApprovedRefreshMetrics = onDocumentUpdated("outputs/{outputId}", async (event) => {
  const before = event.data?.before.data();
  const after = event.data?.after.data();
  if (!before || !after) return;


  if (before.status === "approved" || after.status !== "approved") return;


  const cell = await db.doc(`cells/${after.cellId}`).get();
  const sponsorId = cell.data()?.sponsorId;
  if (!sponsorId) return;


  await db.collection(`cells/${after.cellId}/metrics`).add({
    type: "approved_output",
    outputId: event.params.outputId,
    studentId: after.studentId,
    createdAt: serverTimestamp(),
  });


  await db.doc(`sponsors/${sponsorId}`).update({
    approvedOutputCount: FieldValue.increment(1),
    lastImpactAt: serverTimestamp(),
  });
});
8. Sync user claims from Firestore role

Trigger: Firestore update on users/{userId} if role changes

export const syncUserClaims = onDocumentUpdated("users/{userId}", async (event) => {
  const before = event.data?.before.data();
  const after = event.data?.after.data();
  if (!before || !after) return;
  if (before.role === after.role) return;


  await auth.setCustomUserClaims(event.params.userId, { role: after.role });
});
C. Helper logic notes

Useful helpers to implement:

generateReferralCode(name)
findSponsorByUserId(userId)
getLatestPaidCycleForCell(cellId)
calculateFacilitatorWeeklyAmount(cycleAmount, percent)
determineConnectorMilestone(studentId, cellId)
createNotificationForUser(userId, type, body)
refreshCellProgress(cellId)
nextMondayAtSixPM()
nextPayoutDate()
10) Firebase Storage rules draft
rules_version = '2';
        && request.resource.contentType.matches('image/.*'));
    }


    match /outputs/{cellId}/{studentId}/{fileName} {
      allow read: if isAdmin()
        || (signedIn() && request.auth.uid == studentId)
        || isCellFacilitator(cellId)
        || isCellParent(cellId)
        || (isCellStudent(cellId))
        || isCellSponsor(cellId);


      allow write: if isAdmin()
        || (signedIn() && request.auth.uid == studentId
          && isCellStudent(cellId)
          && request.resource.size < 100 * 1024 * 1024
          && request.resource.contentType.matches('video/.*|audio/.*|image/.*'));
    }


    match /recordings/{cellId}/{sessionId}/{fileName} {
      allow read: if isAdmin()
        || isCellFacilitator(cellId)
        || isCellStudent(cellId)
        || isCellParent(cellId)
        || isCellSponsor(cellId);


      allow write: if isAdmin()
        || (isCellFacilitator(cellId)
          && request.resource.size < 500 * 1024 * 1024
          && request.resource.contentType.matches('video/.*|audio/.*'));
    }


    match /resources/{cellId}/{fileName} {
      allow read: if isAdmin()
        || isCellFacilitator(cellId)
        || isCellStudent(cellId)
        || isCellParent(cellId)
        || isCellSponsor(cellId);


      allow write: if isAdmin() || isCellFacilitator(cellId);
    }


    match /public/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }


    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
D. Storage design notes
For sponsor-facing media, safest approach is not direct raw file exposure.
Better pattern: sponsor reads approved output metadata from Firestore, then app generates controlled display or signed access URL when needed.
Keep upload size checks strict.
Restrict content types tightly.
Separate public marketing assets from private learning assets.
If you want stronger privacy later, remove sponsor direct storage reads and serve sponsor assets through a callable function or signed URL service.
11) Firestore seed JSON + TypeScript interfaces
A. Firestore seed JSON

Use this as a starter seed file for local emulators or first-pass environment setup.

{
      "status": "approved",
      "feedbackCount": 2,
      "approvedBy": "fac_1",
      "approvedAt": "2026-04-06T17:15:00.000Z",
      "reuseCount": 0,
      "visibility": {
        "sponsor": true,
        "cellMembers": true,
        "platform": true
      }
    }
  ],
  "payouts": [
    {
      "id": "pay_001",
      "userId": "user_fac_1",
      "role": "facilitator",
      "cellId": "cell_001",
      "amount": 405000,
      "currency": "VND",
      "type": "weekly",
      "status": "pending",
      "dueDate": "2026-04-10T16:00:00.000Z",
      "notes": "Week 1 payout"
    }
  ],
  "registrations": [
    {
      "id": "reg_001",
      "studentId": "stu_1",
      "cellId": "cell_001",
      "registrationFee": 1000000,
      "currency": "VND",
      "connectorSplit": 500000,
      "platformSplit": 500000,
      "connectorPayoutStatus": "phase_1_paid",
      "platformPayoutStatus": "paid"
    }
  ],
  "sponsorCycles": [
    {
      "id": "cycle_001",
      "sponsorId": "sp_1",
      "cellId": "cell_001",
      "cycleAmount": 25000,
      "currency": "ZAR",
      "studentRewardPercent": 6,
      "sponsorReinvestmentPercent": 9,
      "platformPercent": 20,
      "connectorPercent": 30,
      "facilitatorPercent": 35,
      "status": "paid"
    }
  ],
  "notifications": [
    {
      "id": "noti_001",
      "userId": "user_fac_1",
      "type": "payout_ready",
      "title": "Weekly payout ready",
      "body": "Your week 1 payout has been generated.",
      "read": false
    }
  ]
}
B. Seed loader notes

Recommended seed strategy:

keep one seed.base.json
keep one seed.demo.json
inject createdAt / updatedAt as server timestamps during import
keep IDs explicit for demo environments so UI wiring is predictable
do not seed sensitive payment data beyond safe test values
12) TypeScript interfaces
export type UserRole =
  studentsReferred: number;
  cellsBuilt: number;
  pendingCommissions: number;
  paidCommissions: number;
  status: string;
}


export interface Parent extends BaseDoc {
  userId: string;
  childIds: string[];
  linkedCellIds: string[];
  status: string;
}


export interface Payout extends BaseDoc {
  userId: string;
  role: "facilitator" | "connector" | "student";
  cellId: string;
  amount: number;
  currency: string;
  type: string;
  status: string;
  dueDate: string;
  notes: string;
}


export interface Registration extends BaseDoc {
  studentId: string;
  cellId: string;
  registrationFee: number;
  currency: string;
  connectorSplit: number;
  platformSplit: number;
  connectorPayoutStatus: string;
  platformPayoutStatus: string;
}


export interface SponsorCycle extends BaseDoc {
  sponsorId: string;
  cellId: string;
  cycleAmount: number;
  currency: string;
  studentRewardPercent: number;
  sponsorReinvestmentPercent: number;
  platformPercent: number;
  connectorPercent: number;
  facilitatorPercent: number;
  status: string;
  stripeSessionId?: string;
}


export interface Program extends BaseDoc {
  title: string;
  description: string;
  lessonCount: number;
  status: string;
}


export interface Notification extends BaseDoc {
  userId: string;
  type: string;
  title: string;
  body: string;
  read: boolean;
}
C. Optional stricter types

If you want a stronger codebase later, split these into:

domain.types.ts
firestore.types.ts
api.types.ts
ui.types.ts

Also add:

literal unions for every status
branded IDs like CellId, UserId, SessionId
Zod validators for runtime safety
D. Recommended next code files
// src/types/domain.types.ts
// src/lib/firestore/collections.ts
// src/lib/firestore/converters.ts
// src/lib/seed/seed-base.ts
// src/lib/seed/seed-demo.ts
// src/functions/shared/helpers.ts
13) Firestore converters + seed import script
A. Firestore collection helpers
// src/lib/firestore/collections.ts
import { collection, CollectionReference, DocumentData } from "firebase/firestore";
import { db } from "@/lib/firebase/client";
import {
  User,
  Lead,
  Cell,
  Session,
  Output,
  Sponsor,
  Facilitator,
  Connector,
  Parent,
  Payout,
  Registration,
  SponsorCycle,
  Program,
  Notification,
} from "@/types/domain.types";
import {
  userConverter,
  leadConverter,
  cellConverter,
  sessionConverter,
  outputConverter,
  sponsorConverter,
  facilitatorConverter,
  connectorConverter,
  parentConverter,
  payoutConverter,
  registrationConverter,
  sponsorCycleConverter,
  programConverter,
  notificationConverter,
} from "@/lib/firestore/converters";


export const usersCol = () => collection(db, "users").withConverter(userConverter) as CollectionReference<User>;
export const leadsCol = () => collection(db, "leads").withConverter(leadConverter) as CollectionReference<Lead>;
export const cellsCol = () => collection(db, "cells").withConverter(cellConverter) as CollectionReference<Cell>;
export const sessionsCol = () => collection(db, "sessions").withConverter(sessionConverter) as CollectionReference<Session>;
export const outputsCol = () => collection(db, "outputs").withConverter(outputConverter) as CollectionReference<Output>;
export const sponsorsCol = () => collection(db, "sponsors").withConverter(sponsorConverter) as CollectionReference<Sponsor>;
export const facilitatorsCol = () => collection(db, "facilitators").withConverter(facilitatorConverter) as CollectionReference<Facilitator>;
export const connectorsCol = () => collection(db, "connectors").withConverter(connectorConverter) as CollectionReference<Connector>;
export const parentsCol = () => collection(db, "parents").withConverter(parentConverter) as CollectionReference<Parent>;
export const payoutsCol = () => collection(db, "payouts").withConverter(payoutConverter) as CollectionReference<Payout>;
export const registrationsCol = () => collection(db, "registrations").withConverter(registrationConverter) as CollectionReference<Registration>;
export const sponsorCyclesCol = () => collection(db, "sponsorCycles").withConverter(sponsorCycleConverter) as CollectionReference<SponsorCycle>;
export const programsCol = () => collection(db, "programs").withConverter(programConverter) as CollectionReference<Program>;
export const notificationsCol = () => collection(db, "notifications").withConverter(notificationConverter) as CollectionReference<Notification>;
B. Firestore converters
// src/lib/firestore/converters.ts
  Program,
  Notification,
} from "@/types/domain.types";


function toIso(value: unknown): string | undefined {
  if (!value) return undefined;
  if (typeof value === "string") return value;
  if (value instanceof Timestamp) return value.toDate().toISOString();
  if (typeof value === "object" && value !== null && "toDate" in value && typeof (value as any).toDate === "function") {
    return (value as any).toDate().toISOString();
  }
  return undefined;
}


function baseFromFirestore<T extends { id: string }>(
  snapshot: QueryDocumentSnapshot<DocumentData>,
  data: DocumentData,
): T {
  return {
    id: snapshot.id,
    ...data,
    createdAt: toIso(data.createdAt),
    updatedAt: toIso(data.updatedAt),
  } as T;
}


function baseToFirestore<T extends { id: string }>(value: T): DocumentData {
  const { id, ...rest } = value;
  return rest;
}


function makeConverter<T extends { id: string }>(): FirestoreDataConverter<T> {
  return {
    toFirestore(value: T): DocumentData {
      return baseToFirestore(value);
    },
    fromFirestore(snapshot: QueryDocumentSnapshot, options: SnapshotOptions): T {
      const data = snapshot.data(options);
      return baseFromFirestore<T>(snapshot, data);
    },
  };
}


export const userConverter = makeConverter<User>();
export const leadConverter = makeConverter<Lead>();
export const cellConverter = makeConverter<Cell>();
export const sessionConverter = makeConverter<Session>();
export const outputConverter = makeConverter<Output>();
export const sponsorConverter = makeConverter<Sponsor>();
export const facilitatorConverter = makeConverter<Facilitator>();
export const connectorConverter = makeConverter<Connector>();
export const parentConverter = makeConverter<Parent>();
export const payoutConverter = makeConverter<Payout>();
export const registrationConverter = makeConverter<Registration>();
export const sponsorCycleConverter = makeConverter<SponsorCycle>();
export const programConverter = makeConverter<Program>();
export const notificationConverter = makeConverter<Notification>();
C. Optional stronger converter pattern

Use this later if you want stricter field normalization:

convert missing arrays to []
convert missing nested objects to defaults
map enum-like statuses to exact unions
reject malformed documents early

Example pattern:

function safeArray<T>(value: unknown): T[] {
  return Array.isArray(value) ? (value as T[]) : [];
}

Then inside fromFirestore, normalize fields like:

linkedCellIds
studentIds
parentIds
tags
14) Firebase seed import script
A. Seed script structure
// src/lib/seed/import-seed.ts
    const ref = db.collection(collectionName).doc(record.id);
    batch.set(ref, withTimestamps(record), { merge: true });
    opCount += 1;


    if (opCount === 400) {
      await batch.commit();
      batch = db.batch();
      opCount = 0;
    }
  }


  if (opCount > 0) {
    await batch.commit();
  }
}


async function run() {
  const fileName = process.argv[2] || "./seed.demo.json";
  const payload = loadSeedFile(fileName);


  const orderedCollections = [
    "programs",
    "users",
    "leads",
    "sponsors",
    "facilitators",
    "connectors",
    "parents",
    "cells",
    "sessions",
    "outputs",
    "payouts",
    "registrations",
    "sponsorCycles",
    "notifications",
  ];


  for (const key of orderedCollections) {
    await writeCollection(key, payload[key] || []);
  }


  console.log("Seed import completed.");
}


run().catch((error) => {
  console.error("Seed import failed:", error);
  process.exit(1);
});
B. Example package script
{
  "scripts": {
    "seed:demo": "tsx src/lib/seed/import-seed.ts ./seed.demo.json",
    "seed:base": "tsx src/lib/seed/import-seed.ts ./seed.base.json"
  }
}
C. Example seed file placement
/project-root
  /src
    /lib
      /firestore
        collections.ts
        converters.ts
      /seed
        import-seed.ts
  seed.base.json
  seed.demo.json
D. Safe usage notes
Use demo seed only in staging or emulator first.
Keep explicit IDs for design testing and UI linking.
Never use real sponsor or child data in seed files.
If you need subcollections seeded later, add separate functions for:
cells/{cellId}/metrics
outputs/{outputId}/feedback
users/{userId}/activity
15) Example domain type file
// src/types/domain.types.ts
export * from "@/types/generated-domain";

Or place the full interfaces from Section 12 directly in:

// src/types/domain.types.ts
16) Zod validators + Firebase emulator setup
A. Why add Zod here

Zod gives you runtime validation on top of TypeScript. That matters because:

Firestore can contain malformed data
client forms can send incomplete payloads
imports and webhooks can drift from your interfaces
you want one trusted validation layer before writes

Recommended pattern:

TypeScript interfaces for editor support
Zod schemas for runtime validation
parse on write
safeParse on reads from uncertain sources
17) Zod validators
A. Install
npm install zod

If you want form integration later:

npm install @hookform/resolvers react-hook-form
B. Shared primitives
// src/lib/validators/shared.ts
import { z } from "zod";


export const idSchema = z.string().min(1);
export const isoDateSchema = z.string().datetime().or(z.string().min(1));
export const emailSchema = z.string().email();
export const phoneSchema = z.string().optional().default("");


export const userRoleSchema = z.enum([
  "admin",
  "sponsor",
  "facilitator",
  "student",
  "connector",
  "parent",
]);


export const baseStatusSchema = z.string().min(1);


export const walletSchema = z.object({
  coins: z.number().int().min(0),
  cashRewards: z.number().min(0),
});


export const baseDocSchema = z.object({
  id: idSchema,
  createdAt: z.string().optional(),
  updatedAt: z.string().optional(),
});
C. Core document validators
// src/lib/validators/domain.validators.ts
  studentId: idSchema,
  cellId: idSchema,
  registrationFee: z.number().min(0),
  currency: z.string().min(1),
  connectorSplit: z.number().min(0),
  platformSplit: z.number().min(0),
  connectorPayoutStatus: z.string().min(1),
  platformPayoutStatus: z.string().min(1),
});


export const sponsorCycleSchema = baseDocSchema.extend({
  sponsorId: idSchema,
  cellId: idSchema,
  cycleAmount: z.number().min(0),
  currency: z.string().min(1),
  studentRewardPercent: z.number().min(0),
  sponsorReinvestmentPercent: z.number().min(0),
  platformPercent: z.number().min(0),
  connectorPercent: z.number().min(0),
  facilitatorPercent: z.number().min(0),
  status: z.string().min(1),
  stripeSessionId: z.string().optional(),
});


export const programSchema = baseDocSchema.extend({
  title: z.string().min(1),
  description: z.string().min(1),
  lessonCount: z.number().int().min(1),
  status: z.string().min(1),
});


export const notificationSchema = baseDocSchema.extend({
  userId: idSchema,
  type: z.string().min(1),
  title: z.string().min(1),
  body: z.string().min(1),
  read: z.boolean(),
});


export type UserInput = z.infer<typeof userSchema>;
export type LeadInput = z.infer<typeof leadSchema>;
export type CellInput = z.infer<typeof cellSchema>;
export type SessionInput = z.infer<typeof sessionSchema>;
export type OutputInput = z.infer<typeof outputSchema>;
export type SponsorInput = z.infer<typeof sponsorSchema>;
export type FacilitatorInput = z.infer<typeof facilitatorSchema>;
export type ConnectorInput = z.infer<typeof connectorSchema>;
export type ParentInput = z.infer<typeof parentSchema>;
export type PayoutInput = z.infer<typeof payoutSchema>;
export type RegistrationInput = z.infer<typeof registrationSchema>;
export type SponsorCycleInput = z.infer<typeof sponsorCycleSchema>;
export type ProgramInput = z.infer<typeof programSchema>;
export type NotificationInput = z.infer<typeof notificationSchema>;
D. Validator usage patterns
1. Parse before writing
import { userSchema } from "@/lib/validators/domain.validators";


const payload = userSchema.parse(formValues);
await setDoc(doc(db, "users", payload.id), payload);
2. Safe parse from uncertain source
import { outputSchema } from "@/lib/validators/domain.validators";


const result = outputSchema.safeParse(rawData);
if (!result.success) {
  console.error(result.error.flatten());
  return null;
}


const output = result.data;
3. Validate seed data before import
import { userSchema, cellSchema } from "@/lib/validators/domain.validators";


for (const row of seed.users ?? []) {
  userSchema.parse(row);
}
for (const row of seed.cells ?? []) {
  cellSchema.parse(row);
}
18) Firebase emulator setup
A. Why use emulators first

Use emulators so you can:

test rules safely
seed demo data locally
test Stripe/Firebase logic without touching production
let UI screens run against predictable demo records
B. Required packages
npm install -D firebase-tools tsx

If using Cloud Functions with TypeScript in a separate functions package, keep standard Firebase Functions dependencies there too.

C. firebase.json
{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules"
  },
  "emulators": {
    "auth": {
      "port": 9099
    },
    "functions": {
      "port": 5001
    },
    "firestore": {
      "port": 8080
    },
    "storage": {
      "port": 9199
    },
    "hosting": {
      "port": 5000
    },
    "ui": {
      "enabled": true,
      "port": 4000
    },
    "singleProjectMode": true
  }
}
D. .firebaserc
{
  "projects": {
    "default": "downflow-staging"
  }
}
E. Local app Firebase client config pattern
// src/lib/firebase/client.ts
import { initializeApp, getApps, getApp } from "firebase/app";
import { getAuth, connectAuthEmulator } from "firebase/auth";
import { getFirestore, connectFirestoreEmulator } from "firebase/firestore";
import { getFunctions, connectFunctionsEmulator } from "firebase/functions";
import { getStorage, connectStorageEmulator } from "firebase/storage";


const firebaseConfig = {
  apiKey: process.env.NEXT_PUBLIC_FIREBASE_API_KEY,
  authDomain: process.env.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN,
  projectId: process.env.NEXT_PUBLIC_FIREBASE_PROJECT_ID,
  storageBucket: process.env.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: process.env.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID,
  appId: process.env.NEXT_PUBLIC_FIREBASE_APP_ID,
};


const app = getApps().length ? getApp() : initializeApp(firebaseConfig);


export const auth = getAuth(app);
export const db = getFirestore(app);
export const functions = getFunctions(app);
export const storage = getStorage(app);


if (typeof window !== "undefined" && process.env.NEXT_PUBLIC_USE_FIREBASE_EMULATORS === "true") {
  connectAuthEmulator(auth, "http://127.0.0.1:9099", { disableWarnings: true });
  connectFirestoreEmulator(db, "127.0.0.1", 8080);
  connectFunctionsEmulator(functions, "127.0.0.1", 5001);
  connectStorageEmulator(storage, "127.0.0.1", 9199);
}
F. Emulator environment variables
NEXT_PUBLIC_USE_FIREBASE_EMULATORS=true
NEXT_PUBLIC_FIREBASE_API_KEY=demo-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=downflow-staging.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=downflow-staging
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=downflow-staging.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=1234567890
NEXT_PUBLIC_FIREBASE_APP_ID=1:1234567890:web:abcdef123456

For admin scripts in local dev:

FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099
FIREBASE_STORAGE_EMULATOR_HOST=127.0.0.1:9199
GCLOUD_PROJECT=downflow-staging
G. Package scripts
{
  "scripts": {
    "emulators": "firebase emulators:start",
    "emulators:import": "firebase emulators:start --import=./.firebase-data --export-on-exit",
    "seed:demo": "tsx src/lib/seed/import-seed.ts ./seed.demo.json",
    "seed:base": "tsx src/lib/seed/import-seed.ts ./seed.base.json",
    "dev:local": "NEXT_PUBLIC_USE_FIREBASE_EMULATORS=true next dev"
  }
}
H. First local run sequence
npm run emulators

In a second terminal:

npm run seed:demo

In a third terminal:

npm run dev:local

Then open:

app locally
Emulator UI on port 4000
I. Optional auth seeding script

If you want the UI to log in with demo accounts immediately, create a small script that seeds Auth users in the emulator.

// src/lib/seed/seed-auth.ts
import * as admin from "firebase-admin";


if (!admin.apps.length) {
  admin.initializeApp({ projectId: process.env.GCLOUD_PROJECT || "downflow-staging" });
}


const auth = admin.auth();


async function run() {
  const demoUsers = [
    { uid: "admin_001", email: "admin@downflow.io", password: "Password123!", displayName: "Platform Admin" },
    { uid: "user_fac_1", email: "peter@downflow.io", password: "Password123!", displayName: "Peter Facilitator" },
    { uid: "user_sp_1", email: "jane@acme.com", password: "Password123!", displayName: "Jane Sponsor" },
  ];


  for (const user of demoUsers) {
    try {
      await auth.getUser(user.uid);
    } catch {
      await auth.createUser(user);
    }
  }
}


run().catch(console.error);
J. Recommended local file layout
/src
  /lib
    /firebase
      client.ts
    /firestore
      collections.ts
      converters.ts
    /validators
      shared.ts
      domain.validators.ts
    /seed
      import-seed.ts
      seed-auth.ts
firebase.json
.firebaserc
firestore.rules
firestore.indexes.json
storage.rules
seed.base.json
seed.demo.json
19) Best next files to build
Admin dashboard wireframe text blocks
HubSpot trigger map with exact branching logic
Stripe product catalog + metadata map
Auth seeding + demo login flow
Zod validation wrappers for forms and Cloud Functions

Done — I added the Zod validators + Firebase emulator setup to the canvas.

It now includes:

shared Zod primitives
validators for all core documents
parse and safeParse usage patterns
emulator config files
local Firebase client emulator wiring
env vars
package scripts
first local run sequence
optional auth seed script

Best next step:
Build Admin dashboard wireframe text blocks + HubSpot trigger map